<idea-plugin>
    <id>de.cyan.fca.fdm-plugin</id>

    <name>Fdm</name>

    <vendor email="finncu.cuypers@gmail.com">finncu</vendor>

    <description><![CDATA[
      Toggle Git VCS directory mappings with a single shortcut.

      What it does:
      - Opens a lightweight dialog to quickly enable/disable Git VCS directory mappings per folder.
      - Helps keep large monorepos or multi-module projects fast and focused.
      - Default shortcut: Shift+Alt+G (customizable via Settings > Keymap).

      Deutsch:
      - Öffnet einen leichten Dialog, um Git-Verzeichnismappings schnell zu aktivieren/deaktivieren.
      - Besonders hilfreich für große Monorepos oder Multi‑Module‑Projekte.
      - Standard-Shortcut: Shift+Alt+G (anpassbar über Einstellungen > Keymap).
    ]]></description>

    <change-notes>Compatibility to newer Versions</change-notes>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">

    </extensions>

    <actions>
        <action id="GitVcsToggler.ToggleGitVcsAction" class="de.cyan.fca.ToggleGitVcsAction" text="Toggle Git VCS Mappings" description="Opens a dialog to quickly enable/disable Git VCS directory mappings.">
            <add-to-group group-id="ToolsMenu" anchor="first"/>
            <keyboard-shortcut keymap="$default" first-keystroke="shift alt G"/>
        </action>
    </actions>
</idea-plugin>
